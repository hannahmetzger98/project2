<%= form_with(model: membership_pkg, local: true) do |form| %>
  <% if membership_pkg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(membership_pkg.errors.count, "error") %> prohibited this membership_pkg from being saved:</h2>

      <ul>
        <% membership_pkg.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :amenities %>
    <%= form.text_area :amenities %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
